export default async function handler(req, res) { const { slug } = req.query; const baseUrl = 'https://api.cartola.globo.com'; try { const response = await fetch(`${baseUrl}/${slug}`, { method: req.method, headers: { 'Content-Type': 'application/json', 'User-Agent': 'Mozilla/5.0' }, body: ['POST', 'PUT', 'PATCH'].includes(req.method) ? JSON.stringify(req.body) : null }); const data = await response.json(); res.status(response.status).json(data); } catch (error) { res.status(500).json({ error: 'Failed to fetch from Cartola API', details: error.message }); } }
