export default async function handler(req, res) { if (req.method !== 'POST') { return res.status(405).json({ success: false, error: 'Method not allowed' }); } const { EMAIL, PASSWORD } = process.env; if (!EMAIL || !PASSWORD) { return res.status(500).json({ success: false, error: 'Credentials not configured' }); } try { const response = await fetch('https://login.globo.com/api/authentication', { method: 'POST', headers: { 'Content-Type': 'application/json', 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36' }, body: JSON.stringify({ payload: { email: EMAIL, password: PASSWORD, serviceId: 4728 } }) }); const data = await response.json(); if (response.ok && data.glbId) { return res.status(200).json({ success: true, token: data.glbId }); } return res.status(401).json({ success: false, error: data.error || 'Authentication failed' }); } catch (error) { return res.status(500).json({ success: false, error: 'Network or server error' }); } }
