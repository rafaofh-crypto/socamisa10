export default async function handler(req, res) { if (req.method !== 'POST') { return res.status(405).json({ success: false, error: 'Method not allowed' }); } const email = process.env.EMAIL; const password = process.env.PASSWORD; if (!email || !password) { return res.status(500).json({ success: false, error: 'Missing credentials in environment variables' }); } try { const response = await fetch('https://login.globo.com/api/authentication', { method: 'POST', headers: { 'Content-Type': 'application/json', 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/119.0.0.0 Safari/537.36', 'Origin': 'https://login.globo.com', 'Referer': 'https://login.globo.com/' }, body: JSON.stringify({ payload: { email, password, serviceId: 4728 } }) }); const data = await response.json(); if (response.ok && data.glbId) { return res.status(200).json({ success: true, token: data.glbId }); } else { return res.status(response.status).json({ success: false, error: data.error || 'Authentication failed' }); } } catch (error) { return res.status(500).json({ success: false, error: 'Network or server error: ' + error.message }); } }
